FLL_DESCRIPTION="Java 8- installs openjdk-8 and icedtea."

FLL_PRE_PROCESSING=(
	"#!/bin/bash"
        "echo \"---- in preprocessing ----\""
	"# paste your code here (not necessary for normal install, only for preconfigs)"
        "echo \"---- preprocessing done----\""
)

FLL_PACKAGE_DEPMODS=(
)

FLL_PACKAGES=(
  
      openjdk-8-jre
      openjdk-8-jre-headless
      icedtea-8-plugin 
       
)

