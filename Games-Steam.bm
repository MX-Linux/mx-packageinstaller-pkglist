FLL_DESCRIPTION="Steam"

FLL_PRE_PROCESSING=(
   "#!/bin/bash"
   "echo \"---- in preprocessing ----\""
   "xfce4-terminal -x apt-get install steam"
   "echo \"---- preprocessing done----\""
)

FLL_PACKAGE_DEPMODS=(
)

FLL_PACKAGES=(

)

FLL_POST_PROCESSING=(
	apt-get install -f
)
